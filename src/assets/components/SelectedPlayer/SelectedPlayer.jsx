const SelectedPlayer = ({ choosePlayer }) => {
  return (
    <div>
      <div>
        {choosePlayer.filter((cricketer) => (
          <div> h</div>
        ))}
      </div>
    </div>
  );
};

export default SelectedPlayer;
